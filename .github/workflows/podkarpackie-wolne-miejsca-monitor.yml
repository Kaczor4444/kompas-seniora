name: Monitor wolnych miejsc DPS — Podkarpackie

on:
  schedule:
    # 5. i 20. każdego miesiąca o 10:00 czasu polskiego (8:00 UTC latem / 9:00 UTC zimą)
    - cron: '0 8 5,20 * *'
  workflow_dispatch:
    inputs:
      force:
        description: 'Sprawdź i utwórz Issue nawet jeśli plik się nie zmienił'
        type: boolean
        default: false

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  check-xls:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Zainstaluj zależności
        run: pip install requests xlrd

      - name: Uruchom monitoring wolnych miejsc Podkarpackie
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          FORCE_CHECK: ${{ github.event.inputs.force || 'false' }}
        run: python scripts/monitor-wolne-miejsca-podkarpackie.py

      - name: Zapisz nowy XLS do repozytorium (jeśli jest)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "raw_dane/podkarpackie/" || true
          if git diff --cached --quiet; then
            echo "Brak zmian do zakomitowania"
          else
            git commit -m "chore: Nowy wykaz wolnych miejsc DPS Podkarpackie $(date +%Y-%m-%d)"
            git push
          fi
